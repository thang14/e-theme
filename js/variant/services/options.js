'use strict';

/**
 * @name            OnhanhProduct
 * @description     ProductModel
 */
productModule.provider('VariantOptions', [
  function() {
    var obj = {};
    
    object.themes = [
    
      // Only
      ['color_name'],
      ['size_name'],
      ['style_name'],
      ['configure_name'],
      ['weight_name'],
      ['cover_tyle_name'],
      
      
    ]
    return obj;
  }
]);
